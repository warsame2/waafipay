import 'package:flutter/material.dart';

class BulshoPage extends StatelessWidget {
  const BulshoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bulsho'),
      ),
      body: Center(
        child: Text('Bulsho Page Content'),
      ),
    );
  }
}